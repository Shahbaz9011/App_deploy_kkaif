const express=require("express");const path=require("path");const fs=require("fs");const app=express();const PORT=process.env.PORT||3000;const DATA=path.join(__dirname,"data","requests.json");app.use(express.json({limit:"1mb"}));app.use(express.static(path.join(__dirname,"public")));app.post("/api/requests",(req,res)=>{const required=["name","phone","location","machine"];for(const k of required){if(!req.body[k])return res.status(400).json({message:`${k} is required`})}let rows=[];try{rows=JSON.parse(fs.readFileSync(DATA,"utf8"))}catch{}rows.push({id:Date.now(),receivedAt:new Date().toISOString(),...req.body});fs.writeFileSync(DATA,JSON.stringify(rows,null,2));res.json({ok:true})});app.get("/health",(req,res)=>res.json({ok:true,service:"K-KAIF Machinery Services"}));app.listen(PORT,()=>console.log(`K-KAIF app running at http://localhost:${PORT}`));